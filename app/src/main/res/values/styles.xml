<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary color for top bar and other key elements (use Instagram-like colors) -->
        <item name="colorPrimary">#262626</item>
        <!-- Primary dark color for status bar -->
        <item name="colorPrimaryDark">#000000</item>
        <!-- Accent color for buttons and highlights (e.g., Instagram's gradient) -->
        <item name="colorAccent">#E1306C</item>
        <!-- Background color for screens -->
        <item name="android:windowBackground">#FFFFFF</item>
        <!-- No Action Bar for full-screen experience -->
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Customize navigation bar color -->
        <item name="android:navigationBarColor">#000000</item>
        <!-- Font for Texts -->
        
    </style>

    <!-- Button style similar to Instagram buttons -->
    <style name="AppButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">#E1306C</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">10dp</item>
        <item name="android:gradientRadius">50dp</item>
    </style>

    <!-- Instagram-like text style for titles -->
    <style name="TitleTextStyle" parent="TextAppearance.AppCompat.Large">
        <item name="android:textColor">#262626</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style for smaller text like captions -->
    <style name="CaptionTextStyle" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">#8e8e8e</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
